{
  "name": "videotools",
  "version": "1.0.0",
  "description": "A web application with video tools: seamless loops, image sequences to video, and more",
  "private": true,
  "workspaces": [
    "client"
  ],
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "npm run vercel",
    "vercel": "vercel dev",
    "build": "npm run build --workspace client",
    "test": "npm run test --workspace client"
  },
  "dependencies": {
    "@ffprobe-installer/ffprobe": "^2.1.2",
    "@vercel/blob": "^1.1.1",
    "ffmpeg-static": "^5.2.0",
    "nanoid": "^3.3.6"
  },
  "devDependencies": {
    "@vercel/node": "^5.1.14",
    "typescript": "^5.8.3"
  }
}
